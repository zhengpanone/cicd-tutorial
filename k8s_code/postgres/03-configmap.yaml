# 3. 创建 ConfigMap
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: default
  labels:
    app: postgres
    type: config
data:
  # POSTGRES_DB: mydb
  POSTGRES_USER: postgres
